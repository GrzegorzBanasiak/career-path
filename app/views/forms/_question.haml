.question-item-list
  .button-delete-on-list
    %span{class: "span-delete-from-list", data: { id: question.id} } x
  %h4
    = question.content
  %h5
    Pytanie Obowiązkowe:
    %span.span-in-h5-in-quesion-list
      = question.necessary ? "Tak" : "Nie"

  - if question.is_closed
    .question-option-list{class: "question-nr-" + question.id.to_s }
      %br
      %h5.text-add-new-option Dodaj odpowiedź do pytania:
      %input.input-option-for-question{type: "text", class: "input-for-option-nr-" + question.id.to_s }
      %button.btn.btn-primary.btn-add-option-for-question{data: { question_id: question.id } } +
      - question.question_options.each do |o|
        = render "forms/option", option: o
